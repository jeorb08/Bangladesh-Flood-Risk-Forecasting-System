# ─────────────────────────────────────────────────────────────────────────────
# Bangladesh Flood & Rainfall Forecasting Project
# requirements.txt — Python 3.11 compatible versions (May 2026)
#
# NOTE: Pin Python to 3.11 by adding a .python-version file to your repo root
#       containing just:  3.11
#
# Install everything:
#     pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────


# ── PHASE 1: Data collection & EDA ───────────────────────────────────────────
requests==2.32.3          # NASA POWER API calls
pandas==2.2.3             # DataFrames, time series indexing (3.x not yet stable on 3.11)
numpy==1.26.4             # Numerical ops, sin/cos encoding (2.x has 3.11 issues)
matplotlib==3.9.4         # Basic EDA plots
seaborn==0.13.2           # Heatmaps, distribution plots


# ── PHASE 2: Geospatial (map + district boundaries) ──────────────────────────
geopandas==0.14.4         # Load Bangladesh district GeoJSON polygons
folium==0.18.0            # Interactive choropleth flood risk map
shapely==2.0.6            # Geometry ops (used internally by geopandas)


# ── PHASE 3: Forecasting models ───────────────────────────────────────────────
prophet==1.1.6            # Baseline rainfall forecaster (handles seasonality well)
xgboost==2.1.4            # Main forecasting model + risk classifier
scikit-learn==1.5.2       # Preprocessing, metrics, cross-validation
darts==0.31.0             # N-HiTS / N-BEATS multi-step forecasting (optional stage 2)


# ── PHASE 4: Experiment tracking ─────────────────────────────────────────────
mlflow==2.20.4            # Log runs, hyperparams, RMSE per horizon
optuna==4.2.1             # Hyperparameter tuning for XGBoost


# ── PHASE 5: Explainability ───────────────────────────────────────────────────
shap==0.46.0              # Feature importance + SHAP waterfall plots


# ── PHASE 6: Dashboard & API ─────────────────────────────────────────────────
streamlit==1.44.1         # Main demo dashboard
plotly==5.24.1            # Interactive forecast charts in Streamlit
fastapi==0.115.12         # REST API for /forecast endpoint (optional)
uvicorn==0.34.0           # ASGI server to run FastAPI


# ── UTILITIES ─────────────────────────────────────────────────────────────────
python-dotenv==1.1.0      # Load API keys from .env file
tqdm==4.67.1              # Progress bars for multi-station fetching
joblib==1.4.2             # Save/load trained models (sklearn-compatible)
pyarrow==16.1.0           # Fast parquet read/write for large datasets
